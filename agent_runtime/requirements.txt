# Agent Runtime 核心依赖
pydantic>=2,<3
python-dotenv>=1,<2

# 测试依赖
pytest>=8,<9

# LangGraph 工作流引擎（P0：Agent 状态图编排）
langgraph>=0.2,<1

# OpenAI SDK（DeepSeek / Qwen / GLM 均通过 OpenAI 兼容协议接入）
openai>=1,<2

# Milvus 向量数据库客户端
pymilvus>=2.4,<3
