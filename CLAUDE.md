## 代码结构

- config/：Spring 配置类
- controller/：controllers 层，包含全局控制器异常处理逻辑
- dto/：请求、响应数据传输对象
- entity/：数据库持久化实体类及配套枚举；实体统一使用 Lombok 消除冗余模板代码
- interceptor/：Spring MVC 拦截器
- mapper/：持久层接口，包含 JPA 仓库、数据映射接口
- service/：仅存放业务服务接口
- service/impl/：业务服务接口实现类
- utils/：通用工具类与辅助包，包含自定义异常类

## 编码偏好

- 实体类优先使用 Lombok 自动生成 getter、setter、建造器、构造函数等模板代码
- 控制层保持轻量化，业务逻辑统一下沉至服务层处理
- 状态流转、并发防护、数据审计持久化逻辑均写在 service 实现类，禁止放置在controllers中
- 需要在关键代码中添加注释，例如实体类定义时的字段说明、实现类中业务复杂的代码，确保要有良好的可读性，注释应解释为什么做，而不是做语法解释。
- 新增后端业务模块时，优先沿用现有包分层结构，不随意新增一级根目录

## Git 约束

- 涉及 Git 提交时，提交信息统一使用中文，后续默认保持该约定
